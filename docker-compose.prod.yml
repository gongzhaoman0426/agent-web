services:
  agent-web:
    image: ghcr.io/${GHCR_OWNER}/agent-web:latest
    restart: unless-stopped
    ports:
      - "5179:80"
    networks:
      - agent-api_default

networks:
  agent-api_default:
    external: true
